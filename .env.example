# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Convex (configured when backend integration begins)
NEXT_PUBLIC_CONVEX_URL=
CONVEX_DEPLOYMENT=

# WorkOS AuthKit (configured when auth integration begins)
WORKOS_API_KEY=
WORKOS_CLIENT_ID=
WORKOS_REDIRECT_URI=
WORKOS_COOKIE_PASSWORD=
